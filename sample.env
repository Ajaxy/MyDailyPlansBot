TELEGRAM_BOT_TOKEN=your_bot_token_here

# Option 1: Use DATABASE_URL (Heroku Postgres format)
# SSL is automatically enabled when DATABASE_URL is present
# DATABASE_URL=postgres://username:password@hostname:port/database

# Option 2: Use individual database variables (local development)
# SSL is disabled for local development
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=mdp
